import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "RED Justice — Criminal Network Analysis",
  description:
    "RED Justice — AI-Powered Criminal Network Analysis & Investigation System. Transform heterogeneous evidence into explainable investigation findings.",
  keywords: [
    "RED Justice",
    "investigation",
    "criminal network analysis",
    "knowledge graph",
    "evidence vault",
    "money flow",
    "pattern detection",
    "actor prioritization",
  ],
  authors: [{ name: "RED Justice" }],
  icons: {
    icon: [
      { url: "/icon-32.png", sizes: "32x32", type: "image/png" },
      { url: "/icon-192.png", sizes: "192x192", type: "image/png" },
    ],
    apple: "/icon-192.png",
  },
  openGraph: {
    title: "RED Justice — Criminal Network Analysis",
    description: "AI-Powered Criminal Network Analysis & Investigation System",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
